# secrt-manager

only run in ec2

#Create a folder in opt directory named terraform-demo-secrets and switch to that folder.
	
1) mkdir /opt/terraform-demo-secrets
2) cd /opt/terraform-demo-secrets

create main.tf and provider.tf insite the /opt/terraform-demo-secrets


#first you need to do aws configure 
#terraform init
#terraform plan
#terraform apply
